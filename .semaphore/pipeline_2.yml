version: v1.0
name: Pipeline 2
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
blocks:
  - name: 'Block #1'
    task:
      jobs:
        - name: 'Job #1'
          commands:
            - echo $env_var1
            - echo $env_var2
            - 'git clone https://github.com/mateuscap/public.git'
            - cd public/
            - git checkout dummy-branch
            - git merge origin/rebase-through-promotion
            - git push
