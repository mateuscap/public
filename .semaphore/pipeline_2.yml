version: v1.0
name: Pipeline 2
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
blocks:
  - name: 'Block #1'
    task:
      jobs:
        - name: 'Job #1'
          commands:
            - 'git clone https://github.com/mateuscap/public.git'
            - cd public/
            - git config --global user.email "mcapistrano@dummy.com"
            - git config --global user.name "Mateus"
            - git checkout dummy-branch
            - git merge origin/rebase-through-promotion -m "message"
            - git push
